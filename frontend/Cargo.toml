[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
shared = { path = "../shared" }
leptos = { version = "0.6", features = ["csr"] }
reqwasm = "0.5"
serde = { version = "1.0", features = ["derive"] }
console_error_panic_hook = "0.1"